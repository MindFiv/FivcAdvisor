# Environment & Secrets

- Configure models via env vars: `DEFAULT_LLM_*`, `REASONING_LLM_*`, `CHAT_LLM_*`, `CODING_LLM_*`
- Do not commit secrets; use `.env` for local dev (see `.env.example`)
- The CLI and flows pick up env via `llms.py` factories
